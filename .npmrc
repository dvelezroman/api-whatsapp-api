# Lock package versions for consistent deployments
package-lock=true
save-exact=true

# Use npm registry
registry=https://registry.npmjs.org/

# Security settings
audit-level=moderate
fund=false

# Performance settings
prefer-offline=true
cache-min=86400
