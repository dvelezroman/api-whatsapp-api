version: '3.8'

services:
  whatsapp-api:
    build: .
    container_name: whatsapp-api
    restart: always
    ports:
      - "3005:3005"
    volumes:
      - ./whatsapp-session:/app/whatsapp-session
    environment:
      - NODE_ENV=production
